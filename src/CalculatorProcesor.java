public class CalculatorProcesor {
    public double calculateResult(double base1, double power1, double base2, double power2) {
        double result = 0;
        /*
            Spocti result = ( base1 ^ power1 ) + (base2 ^ power2).
            Kde kazde pocitani v zavorkach (..) je pocitana ve vlasnim vlakne pouzitim metody calculate() tridy ItemCalculator
        */

        return result;
    }
}
